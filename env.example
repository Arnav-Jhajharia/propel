# Database
DATABASE_URL=file:./local.db
# For Turso (cloud SQLite) - uncomment and use your credentials
# DATABASE_URL=libsql://your-database-name.turso.io
# DATABASE_AUTH_TOKEN=your-auth-token

# Encryption (REQUIRED for production)
# Generate a strong 32-character key: openssl rand -hex 32
ENCRYPTION_KEY=your-32-character-secret-encryption-key-here

# OpenAI / LLMs
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-4o-mini

# LLM Provider Selection (openai or dedalus)
LLM_PROVIDER=openai

# Dedalus Labs (Python bridge service)
# Set LLM_PROVIDER=dedalus to use Dedalus Labs
# Requires Python bridge service running (see dedalus-bridge/README.md)
DEDALUS_API_KEY=your-dedalus-api-key
DEDALUS_MODEL=openai/gpt-5-mini
DEDALUS_BRIDGE_URL=http://localhost:8001
TOOL_EXECUTOR_URL=http://localhost:3001/api/tools/execute

# MCP Server (Optional - for direct backend access)
# Set USE_MCP_SERVER=true to use MCP server instead of HTTP callbacks
# Requires MCP server running (see mcp-server/README.md)
USE_MCP_SERVER=false
MCP_SERVER_URL=local/rental-dashboard-mcp

# WhatsApp Business API (Legacy - now stored per-user in database)
# WHATSAPP_TOKEN=your-whatsapp-token
# WHATSAPP_PHONE_NUMBER_ID=your-phone-number-id
WHATSAPP_VERIFY_TOKEN=dev-verify-token

# Better Auth
BETTER_AUTH_SECRET=your-secret-key-here
BETTER_AUTH_URL=http://localhost:3000

# Optional: For production
# NEXTAUTH_URL=https://your-domain.com
# NEXTAUTH_SECRET=your-nextauth-secret

# Clerk (Authentication)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxx
CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxx
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# Google Translate API (optional for auto-translate)
GOOGLE_TRANSLATE_API_KEY=
